


def construct_extract_data():
    """Constructs and returns the state graph for extracting data from step 1."""
    workflow = StateGraph(ExtractMainDataState)
    graph = None

    return graph